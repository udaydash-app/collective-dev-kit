export default function Ledgerly() {
  return (
    <iframe
      src="https://grainflow-accounting.lovable.app"
      title="Ledgerly"
      className="w-full h-full border-0"
      style={{ width: '100%', height: '100%', minHeight: '100%' }}
      allow="clipboard-read; clipboard-write; fullscreen"
    />
  );
}